# =============================================================================
# Extra pip packages baked into the custom Airflow Docker image
# (on top of the base apache/airflow image which already has Airflow itself)
# =============================================================================

# Airflow providers — versions are constrained by the Airflow constraints file
# in the Dockerfile, so no pins needed here.
apache-airflow-providers-apache-spark
apache-airflow-providers-amazon

# Core deps (also in requirements.txt for local dev parity)
# requests, boto3, botocore are omitted here — they are provided transitively
# by the Airflow base image and apache-airflow-providers-amazon respectively.
# Pinning them here creates a three-way conflict the pip resolver cannot escape.
gql[requests]==3.5.0
pyarrow==16.1.0
pyspark==3.5.1

# dbt + Trino adapter are installed in an isolated virtualenv (/opt/airflow/dbt-venv)
# in the Dockerfile to avoid a protobuf version conflict with Airflow 2.9.3.
